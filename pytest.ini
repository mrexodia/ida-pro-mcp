[pytest]
markers =
    requires_ida: test only runs against a real IDA/idalib runtime; auto-skipped in a headless environment (the stub idaapi in tests/conftest.py is active). Applied to the src/ida_pro_mcp/ida_mcp/tests/ runtime suite and the supervisor import-isolation test.
